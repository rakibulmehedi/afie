# PostgreSQL — worker_rw role (RLS enforced; tenant_admin never used here)
DATABASE_URL=

# QStash — signature verification
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# LLM — worker-only, never bundled to edge
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
